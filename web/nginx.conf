server {
    listen 80;
    location /api/ {
        proxy_pass http://api:8000/api/;
        proxy_set_header Host $host;
    }
    location / {
        return 200 'microservices portfolio demo\n';
        add_header Content-Type text/plain;
    }
}
