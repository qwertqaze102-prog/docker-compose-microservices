server {
    listen 80;
    location /api/ {
        proxy_pass http://api:8000/api/;
        proxy_set_header Host $host;
    }
    location / {
        default_type text/plain;
        return 200 'microservices lab\nPOST /api/jobs {"task":"ping"}\nGET /api/health\n';
    }
}
